import 'package:flutter/material.dart';

//void main() => runApp(Social());

class Social extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Social',
        style: TextStyle(fontSize: 30.0,color:Colors.indigo,fontWeight: FontWeight.bold ),),
    );
  }
}
